# infra, deployment, cicd Grill 세션

- 날짜: 2026-07-08
- 케이스: architecture-decision-grill
- 스킬 설치 루트: /Users/bhoon/Project/jbnu-sugang-helper/.agents/skills/ultra-grill-me
- 상태: 진행 중

## 요약

인프라, 배포 환경, CI/CD를 비용 최소화와 유지보수 용이성 기준으로 어떻게 묶을지 검증하는 중입니다.

## 질문 로그

### Q1. `web`을 계속 Vercel에 둘까요, 아니면 OCI로 같이 옮길까요?

- 막힌 결정: 프론트엔드를 별도 PaaS로 유지할지, 서버/인프라와 같은 OCI 운영 경계로 합칠지 정해지지 않았습니다.
- 제공된 옵션:
  - 1. (추천) `web`은 Vercel 유지, `server/infra`만 OCI 유지
  - 2. `web`도 OCI로 옮겨서 모두 한 운영 환경으로 통합
  - 3. 다른 옵션 더 추천받기
  - 4. 직접 답변
- 답변: 3
- 결과: 거부
- 해석 결과: 기존 2안/1안보다 다른 대안을 더 보고 싶다는 의미로 해석
- 새로 확정된 결정: 없음
- 남은 열린 질문: Vercel 유지와 OCI 통합 사이의 현실적 중간안

### Q2. 운영 복잡도를 줄이기 위한 중간안으로 어떤 방향이 가장 맞나요?

- 막힌 결정: `web`을 OCI로 완전히 옮기지 않으면서도, 저장소/CI/배포 복잡도를 줄일 수 있는 설계가 정해지지 않았습니다.
- 제공된 옵션:
  - 1. (추천) `web`은 Vercel 유지, 저장소만 하나로 합치는 monorepo
  - 2. `web`은 Vercel 유지, 저장소는 분리 유지하되 API 계약만 강하게 고정
  - 3. `web`도 OCI로 옮기되, 먼저 정적 자산만 OCI로 두는 점진 이전
  - 4. 다른 옵션 더 추천받기
  - 5. 직접 답변
- 답변: 
- 답변: 2
- 결과: 확정
- 해석 결과: 일정 시간 모니터링 후 수동 판단
- 새로 확정된 결정: 롤백은 관찰 후 수동으로 판단
- 남은 열린 질문: 모니터링 관찰 창

### Q14. 컷오버 후 얼마나 모니터링할까요?

- 막힌 결정: 수동 판단을 하더라도 관찰 창이 없으면 언제 결론을 내릴지 정할 수 없습니다.
- 제공된 옵션:
  - 1. (추천) 30분 집중 모니터링
  - 2. 2시간 업무 시간 모니터링
  - 3. 다음 날까지 느긋하게 관찰
  - 4. 다른 옵션 더 추천받기
  - 5. 직접 답변
- 답변: 3
- 결과: 확정
- 해석 결과: 다음 날까지 느긋하게 관찰
- 새로 확정된 결정: 관찰 창은 다음 날까지
- 남은 열린 질문: 롤백 판단 신호

### Q15. 롤백 판단 신호는 무엇으로 둘까요?

- 막힌 결정: 관찰 창이 길어지면, 어떤 증상이 나오면 되돌릴지 기준이 필요합니다.
- 제공된 옵션:
  - 1. (추천) 인증/로그인/주요 페이지 500 또는 4xx 반복
  - 2. 배포 직후 모든 경고성 에러
  - 3. 사용자가 불편 제보하면 수동 판단
  - 4. 다른 옵션 더 추천받기
  - 5. 직접 답변
- 답변: 1
- 결과: 확정
- 해석 결과: 인증/로그인/주요 페이지에서 500 또는 4xx가 반복되면 롤백 판단
- 새로 확정된 결정: 롤백 판단 신호는 핵심 경로의 반복 오류
- 남은 열린 질문: 없음

### Q3. 세 가지 중간안은 각각 비용과 유지보수에서 어떤 장단점이 있나요?

- 막힌 결정: 어떤 중간안이 비용 최소화와 운영/유지보수 용이성에 더 맞는지 비교 기준이 필요했습니다.
- 제공된 옵션:
  - 1. `web`은 Vercel 유지, 저장소만 하나로 합치는 monorepo
  - 2. `web`은 Vercel 유지, 저장소는 분리 유지하되 API 계약만 강하게 고정
  - 3. `web`도 OCI로 옮기되, 먼저 정적 자산만 OCI로 두는 점진 이전
  - 4. 다른 옵션 더 추천받기
  - 5. 직접 답변
- 답변: 1번으로 할게. vercel 유지 및 github 레포지토리 통합(infra, web, server)
- 결과: 확정
- 해석 결과: `web`은 Vercel에 남기고, `web/server/infra`는 하나의 GitHub monorepo로 통합하겠다는 결정
- 새로 확정된 결정: 배포 플랫폼은 `web=Vercel`, `server+infra=OCI`, 저장소는 단일 monorepo
- 남은 열린 질문: monorepo에서 CI와 배포 경계를 어떻게 나눌지

### Q4. monorepo에서 CI를 경로별로 나눌까요, 아니면 하나로 통합할까요?

- 막힌 결정: 저장소를 합친 뒤 CI를 모든 변경에 대해 전부 돌릴지, 변경 영역만 골라 돌릴지 정해지지 않았습니다.
- 제공된 옵션:
  - 1. (추천) 경로 기반으로 분리된 CI: `web/`, `server/`, `infra/` 변경만 해당 job 실행
  - 2. 하나의 통합 CI: 모든 push마다 전부 검사
  - 3. 공통 검사만 항상 돌리고, 배포 관련 job만 경로별로 분리
  - 4. 다른 옵션 더 추천받기
  - 5. 직접 답변
- 답변: 경로기반 분리
- 결과: 확정
- 해석 결과: `web/`, `server/`, `infra/` 변경만 각 job이 실행되는 path-based CI를 택함
- 새로 확정된 결정: monorepo 내 CI는 경로 기반 분리
- 남은 열린 질문: monorepo 디렉터리 구조를 어떻게 표준화할지

### Q5. monorepo 디렉터리 구조는 어떤 형태가 더 맞나요?

- 막힌 결정: 저장소를 합친 뒤 각 영역을 어떤 폴더 규칙으로 배치할지 정해지지 않았습니다.
- 제공된 옵션:
  - 1. (추천) 루트에 `web/`, `server/`, `infra/`를 그대로 두는 최소 변경 구조
  - 2. `apps/web`, `apps/server`, `infra` 같은 표준 monorepo 구조
  - 3. `frontend/`, `backend/`, `ops/`처럼 역할명으로 바꾸는 구조
  - 4. 다른 옵션 더 추천받기
  - 5. 직접 답변
- 답변: 
- 결과: 
- 해석 결과: 
- 새로 확정된 결정: 
- 남은 열린 질문: 

### Q6. 표준 monorepo 구조는 어떤 패턴들이 있나요?

- 막힌 결정: 경로 기반 CI를 유지하면서도, 장기 유지보수에 유리한 표준 폴더 구조 후보를 비교할 필요가 있었습니다.
- 제공된 옵션:
  - 1. `apps/web`, `apps/server`, `infra` 중심 구조
  - 2. 루트에 `web/`, `server/`, `infra/`를 그대로 두는 구조
  - 3. `frontend/`, `backend/`, `ops/` 역할 분리 구조
  - 4. `apps/` + `packages/` 혼합 구조
  - 5. 직접 답변
- 답변: 1. apps/web, apps/server, infra -> 이거로 할게
- 결과: 확정
- 해석 결과: monorepo의 표준형 앱 디렉터리는 `apps/` 아래로 두고, 인프라는 별도 `infra/`로 두겠다는 의미
- 새로 확정된 결정: `apps/web`, `apps/server`, `infra` 구조를 채택
- 남은 열린 질문: 공용 코드를 처음부터 분리할지 여부

### Q7. 공용 코드/타입 패키지를 지금부터 둘까요, 아니면 나중에 필요할 때 추가할까요?

- 막힌 결정: `apps/web`와 `apps/server` 사이에서 공유할 코드(타입, API client, 유틸)를 지금부터 구조에 넣을지 정해지지 않았습니다.
- 제공된 옵션:
  - 1. (추천) 처음부터 `packages/shared` 또는 `packages/api-client`를 둠
  - 2. 지금은 `apps/`와 `infra/`만 두고, 공용 코드는 나중에 추가
  - 3. 다른 옵션 더 추천받기
  - 4. 직접 답변
- 답변: 1
- 결과: 확정
- 해석 결과: 처음부터 공용 코드/타입 패키지를 둘 것
- 새로 확정된 결정: `packages/shared` 또는 `packages/api-client`를 포함하는 방향
- 남은 열린 질문: 공용 패키지의 1차 책임을 무엇으로 둘지

### Q8. 공용 패키지의 1차 책임은 무엇으로 두는 게 맞나요?

- 막힌 결정: 공용 패키지를 만든다고 해도, 첫 번째 책임을 어디에 둘지 정해야 유지보수 경계가 선명해집니다.
- 제공된 옵션:
  - 1. (추천) API 타입 + client 생성만 담당하는 `packages/api-client`
  - 2. 타입, 유틸, 상수까지 넓게 담는 `packages/shared`
  - 3. 다른 옵션 더 추천받기
  - 4. 직접 답변
- 답변: 2
- 결과: 확정
- 해석 결과: 공용 패키지는 타입, 유틸, 상수까지 넓게 담는 `packages/shared`로 두겠다는 의미
- 새로 확정된 결정: `packages/shared`를 둔다
- 남은 열린 질문: 없음

### Q9. monorepo 이관은 어떤 순서로 시작하는 게 안전한가요?

- 막힌 결정: `web`, `server`, `infra`를 하나로 합칠 때 어디부터 옮길지 정해야 실제 마이그레이션 계획을 세울 수 있습니다.
- 제공된 옵션:
  - 1. (추천) `web` 먼저 이전
  - 2. `server` 먼저 이전
  - 3. `infra` 먼저 이전
  - 4. 다른 옵션 더 추천받기
  - 5. 직접 답변
- 답변: 5. web - server - infra 순으로
- 결과: 확정
- 해석 결과: 이관 순서를 `web` → `server` → `infra`로 잡음
- 새로 확정된 결정: 마이그레이션 순서는 `web`, `server`, `infra`
- 남은 열린 질문: `web` 이관의 컷오버 전략

### Q10. `web` 이관은 어떤 컷오버 전략으로 갈까요?

- 막힌 결정: `web`을 monorepo로 옮길 때 기존 repo와 새 repo를 얼마나 병행할지 정해야 배포 중단과 롤백 부담을 줄일 수 있습니다.
- 제공된 옵션:
  - 1. (추천) 새 monorepo를 먼저 준비하고, Vercel을 한 번에 새 경로로 컷오버
  - 2. 기존 `web` repo와 새 monorepo를 잠시 병행 운영 후 천천히 전환
  - 3. 다른 옵션 더 추천받기
  - 4. 직접 답변
- 답변: 1
- 결과: 확정
- 해석 결과: 새 monorepo를 먼저 준비한 뒤 Vercel을 한 번에 새 경로로 컷오버
- 새로 확정된 결정: `web` 이관은 원샷 컷오버
- 남은 열린 질문: `web` 이관 시 반드시 보존해야 할 것

### Q11. `web` 이관에서 반드시 보존해야 하는 것은 무엇인가요?

- 막힌 결정: `web` 이관 동안 깨지면 안 되는 범위를 알아야 롤백 기준과 검증 범위를 정할 수 있습니다.
- 제공된 옵션:
  - 1. (추천) 사용자-facing URL, 인증 흐름, 배포 환경변수만 보존
  - 2. UI 구조와 라우트 구조까지 모두 보존
  - 3. 다른 옵션 더 추천받기
  - 4. 직접 답변
- 답변: 1
- 결과: 확정
- 해석 결과: 사용자-facing URL, 인증 흐름, 배포 환경변수만 보존하면 된다는 의미
- 새로 확정된 결정: `web` 이관 시 URL, auth, env는 유지
- 남은 열린 질문: Vercel 프로젝트를 유지할지 새로 만들지

### Q12. `web`의 Vercel 프로젝트는 기존 것을 유지할까요, 새로 만들까요?

- 막힌 결정: 원샷 컷오버를 할 때 Vercel 배포 식별자를 유지할지 새로 분리할지 정해야 실제 전환 절차가 정해집니다.
- 제공된 옵션:
  - 1. (추천) 기존 Vercel 프로젝트 유지, repo 연결만 새 monorepo로 전환
  - 2. 새 Vercel 프로젝트 생성 후 도메인만 이전
  - 3. 다른 옵션 더 추천받기
  - 4. 직접 답변
- 답변: 1
- 결과: 확정
- 해석 결과: 기존 Vercel 프로젝트를 그대로 유지하고 repo 연결만 새 monorepo로 전환
- 새로 확정된 결정: Vercel 프로젝트는 재사용
- 남은 열린 질문: 배포 실패 시 되돌림 기준

### Q13. `web` 컷오버 실패 시 되돌림 기준은 어떻게 둘까요?

- 막힌 결정: 원샷 컷오버는 실패 시 즉시 되돌릴 기준이 필요합니다.
- 제공된 옵션:
  - 1. (추천) 배포 후 주요 URL/인증 흐름 실패가 보이면 즉시 이전 Vercel 연결로 롤백
  - 2. 일정 시간 동안 모니터링 후 수동 판단
  - 3. 다른 옵션 더 추천받기
  - 4. 직접 답변
- 답변: 2
- 결과: 확정
- 해석 결과: 일정 시간 모니터링 후 수동 판단
- 새로 확정된 결정: 롤백은 관찰 후 수동으로 판단
- 남은 열린 질문: 모니터링 관찰 창

### Q14. 컷오버 후 얼마나 모니터링할까요?

- 막힌 결정: 수동 판단을 하더라도 관찰 창이 없으면 언제 결론을 내릴지 정할 수 없습니다.
- 제공된 옵션:
  - 1. (추천) 30분 집중 모니터링
  - 2. 2시간 업무 시간 모니터링
  - 3. 다음 날까지 느긋하게 관찰
  - 4. 다른 옵션 더 추천받기
  - 5. 직접 답변
- 답변: 3
- 결과: 확정
- 해석 결과: 다음 날까지 느긋하게 관찰
- 새로 확정된 결정: 관찰 창은 다음 날까지
- 남은 열린 질문: 롤백 판단 신호

### Q15. 롤백 판단 신호는 무엇으로 둘까요?

- 막힌 결정: 관찰 시간이 길어지면, 어떤 증상이 나오면 되돌릴지 기준이 필요합니다.
- 제공된 옵션:
  - 1. (추천) 인증/로그인/주요 페이지 500 또는 4xx 반복
  - 2. 배포 직후 모든 경고성 에러
  - 3. 사용자가 불편 제보하면 수동 판단
  - 4. 다른 옵션 더 추천받기
  - 5. 직접 답변
- 답변: 1
- 결과: 확정
- 해석 결과: 인증/로그인/주요 페이지에서 500 또는 4xx가 반복되면 롤백 판단
- 새로 확정된 결정: 롤백 판단 신호는 핵심 경로의 반복 오류
- 남은 열린 질문: 없음

## 확정된 결정

- 비용 최소화와 운영/유지보수 용이성을 최우선 기준으로 본다.
- `web`은 Vercel에 유지한다.
- `web`, `server`, `infra`는 하나의 GitHub monorepo로 통합한다.
- monorepo CI는 경로 기반으로 분리한다.
- monorepo 구조는 `apps/web`, `apps/server`, `infra`로 둔다.
- 공용 코드/타입 패키지를 처음부터 둔다.
- 공용 패키지는 타입, 유틸, 상수까지 담는 `packages/shared`로 둔다.
- 마이그레이션 순서는 `web` → `server` → `infra`다.
- `web` 이관은 원샷 컷오버로 한다.
- `web` 이관 시 URL, auth, env는 유지한다.
- Vercel 프로젝트는 재사용한다.
- 롤백은 관찰 후 수동으로 판단한다.
- 관찰 창은 다음 날까지 둔다.
- 롤백 신호는 인증/로그인/주요 페이지의 반복 500 또는 4xx 오류다.
- 롤백은 관찰 후 수동으로 판단한다.
- 관찰 창은 다음 날까지 둔다.
- 롤백 신호는 인증/로그인/주요 페이지의 반복 500 또는 4xx 오류다.

## 임시 가정

- 없다.

## 남은 known unknowns

- `web`의 배포 플랫폼을 어디에 둘지.

## 최종 정리

[종료 조건 충족 시 최종 정리 내용을 여기에 기록]
