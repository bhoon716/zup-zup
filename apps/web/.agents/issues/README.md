# Issues

See [index.md](index.md) for the tracker conventions.

Active issues should live under `open/`, `in-progress/`, or `blocked/`.
Closed imported history lives under `closed/`.
Follow the execution rules in `index.md`: minimal scope, explicit assumptions, and targeted verification.
