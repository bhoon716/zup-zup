# Project Release Notes

This document merges the web and server release histories.

## Web Release Notes

`web` 모듈의 정식 릴리스 UI/UX 명세입니다.

---

## Unreleased

### Fixed

- **알림 delivery 중복·DLQ 운영 경계**: delivery마다 안정적인 key와 lease fencing을 적용해 stale worker의 상태 덮어쓰기를 막고, provider별 중복 완화 key를 전파합니다. 단일 관리자는 전체 DLQ를 안전한 상태 정보로 조회하고 같은 delivery/key만 선택 재처리할 수 있으며, `SENT` 재발송은 기본 차단되고 모든 replay는 감사됩니다. DLQ는 실제 전환 시점부터 최소 30일 보존합니다.
- **기기 해제 토큰 경로 제거 준비**: 웹 클라이언트는 토큰을 URL이 아닌 `DELETE /api/v1/users/devices` 요청 본문으로 전송하도록 변경했습니다. 기존 `DELETE /api/v1/users/devices/token/{token}` 경로는 배포된 구 클라이언트 호환을 위해 소유자 범위 검사를 거쳐 한시적으로 유지하며, 접근 로그와 웹 번들에서 사용이 사라진 다음 breaking API 릴리스에서 제거합니다.
- **민감값 로그·오류 응답 보호**: 알림·크롤러·OAuth 로그는 이메일 마스킹, 토큰/endpoint 지문, 안정 오류 코드만 남기고 HTTP query 값은 기록하지 않습니다. 공개 오류 응답은 원문 상세 대신 `X-Error-Id`/`correlationId`를 제공해 서버 로그와 대조할 수 있습니다.
- **운영 진단 경계 축소**: 운영 actuator는 별도 8081 management port에서 `health`·`prometheus`만 제공하며, 앱→Prometheus→Grafana 내부망으로 격리됩니다. Swagger/OpenAPI·H2 console은 운영에서 비활성화되고, CORS는 명시 HTTPS origin만 credential 요청에 허용합니다. 다음 운영 배포 전 `SERVER_DOTENV`의 `APP_CORS_ALLOWED_ORIGINS`를 실제 서비스 HTTPS origin으로 갱신해야 합니다.
- **Flyway 검증 분리**: 기본 `./gradlew test`는 빠른 피드백을 위해 migration 검증을 제외하고, `./gradlew migrationTest`가 MySQL fresh schema·checksum·upgrade path를 별도 report로 검증합니다. PR과 main 배포 전 CI는 Docker preflight 후 두 task를 모두 필수 실행합니다.
- **Redis JWT 원문 저장 제거**: 새 access blacklist는 SHA-256 식별자만 저장하고, refresh registry는 무작위 family와 SHA-256 digest를 Lua CAS로 교체합니다. 이전 refresh 재사용은 같은 family를 폐기하며, Spring Session에는 JWT 원문 대신 인증 주체·권한·access 만료 시각만 저장됩니다. 배포 이전 raw refresh/session 값은 각각 기존 TTL 안에서 자연 정리됩니다.
- **Redis 준비·복구 경계 강화**: Redis는 인증 `PING` healthcheck와 AOF host storage를 사용하고, 앱은 Redis와 DB가 포함된 internal readiness가 `UP`일 때만 기동합니다. Redis 요청은 연결·명령 2초 제한 안에서 재연결하되 단절 중 명령을 queue/replay하지 않습니다. 정상 restart에는 session·refresh·logout blacklist·dedupe 상태가 보존되며, 백업·검증된 restore drill과 배포 전 Redis preflight를 제공합니다.
- **DB 최소 권한·재해 복구**: runtime app은 DML 전용 MySQL 계정만 받고, Flyway는 배포 전 일회성 migrator container로 분리합니다. root·migrator·backup credential은 runtime JVM에 남지 않습니다. 암호화된 DR archive는 MySQL dump/binlog·uploads·Grafana·NPM data/cert와 SHA-256/HMAC 검증을 보관하며, clean-host drill이 사용자 식별 레코드·구독·피드백 첨부파일·운영 상태를 복구합니다. 세부 정책과 실제 OAuth 확인 경계는 [disaster-recovery-policy.md](disaster-recovery-policy.md)에 기록합니다.
- **탈퇴 즉시 차단·데이터 보존 전환**: 탈퇴하면 인증 cookie/session·refresh registry·기기 token을 즉시 폐기하고 이름·이메일·알림 이메일·Discord 식별자를 익명화합니다. 계정과 일반 이력은 soft delete로 보존하고 구독·알림은 즉시 중지하며, feedback과 첨부파일은 일반 경로에서 숨깁니다. 기존 탈퇴 계정은 복구하지 않으며 같은 Google 이메일로 다시 로그인하면 새 계정으로 시작합니다. 보안상 배포 전 UID 없는 인증은 한 번 재로그인이 필요합니다.
- **구조화된 관리자 감사 로그**: 관리자 상태·답변·첨부파일 접근을 버전 있는 JSON envelope로 기록하고, 답변 원문 대신 길이·키 기반 HMAC-SHA-256 지문만 보존합니다. `GET /api/v1/admin/audit-logs`는 관리자만 최신순 페이지 조회할 수 있으며, V18이 기존 원문형 metadata를 안전한 legacy 표기로 정리합니다.
- **삭제 피드백 관리자 보존 열람**: 단일 관리자는 활성·삭제 피드백을 상태별로 조회할 수 있고, 탈퇴 작성자는 `탈퇴 사용자`로만 표시됩니다. 목록·상세 JSON에서 직접 식별자·환경 메타 정보·첨부파일 이름/URL을 제외하며, 첨부파일은 화면 확인 뒤 전용 POST 요청으로만 내려받고 성공 열람을 감사 로그에 기록합니다. 수동 피드백·답변 삭제도 물리 삭제 대신 보존용 soft delete를 사용합니다.
- **피드백 환경 메타데이터 최소화·검증**: 새 피드백은 `os`·`language`만 최대 512 UTF-8 bytes 범위에서 전송·저장합니다. 서버는 JSON object·키·값 타입·중복 키·trailing token을 저장 전에 검증하고, 손상된 multipart JSON이나 metaInfo는 `400/G002`로 반환합니다. 전체 URL·user agent·timestamp 수집은 제거했으며 관리자 응답의 metaInfo 비노출 경계는 유지합니다.
- **피드백 첨부 이미지 안전 정규화**: 서버는 multipart ingress에서 파일 10 MiB·요청 11 MiB를 먼저 제한하고, 초과는 `F008/413`으로 반환합니다. Tika sniff 뒤 실제 decoder로 정적 JPEG·PNG만 읽어 한 변 4096·8,388,608 pixels·정규화 결과 5 MiB를 제한합니다. WebP·GIF·APNG는 거부하며, 통과한 이미지는 새 JPEG/PNG로 재인코딩해 EXIF/GPS/XMP·PNG text 등 원본 metadata를 제거하고 다운로드 이름도 실제 정규화 확장자를 사용합니다.
- **인증된 관리자 첨부파일 미리보기**: 단일 관리자는 명시 확인 POST로 받은 blob만 object URL로 미리보고 같은 blob을 다운로드합니다. 서버는 저장 파일의 실제 bytes를 다시 sniff해 JPEG/PNG만 image content type으로 내보내며, legacy WebP/GIF·알 수 없는 파일은 inline preview 없이 다운로드합니다. feedback–attachment 쌍 조회·감사 로그 경계는 유지하고, 선택 전환·페이지 이탈 뒤 늦게 도착한 요청은 object URL을 만들지 않습니다.
- **웹 런타임 안정화**: Next.js와 `eslint-config-next`를 matching `16.3.0-preview.5`로 고정합니다. 현재 stable `16.2.10`이 취약한 PostCSS를 직접 고정해 preview를 임시 production 예외로 유지하며, 루트 workspace lockfile·Node 22.x를 CI/배포 기준으로 통일하고 매주 production dependency audit을 실행합니다.

---

## [v1.2.3] - 2026-06-16 ✨

메인 랜딩 페이지에 생생한 사용자 후기 무한 Marquee 슬라이더가 도입되었습니다.

### Added

- **생생한 사용자 후기 무한 슬라이더**: 메인 페이지 하단에 실제 에브리타임에서 작성된 학생들의 생생한 후기들을 롤링하는 무한 Marquee 슬라이더 구현
- **인터랙티브 디자인**: 마우스 오버 시 슬라이더 자동 정지 기능 및 좌우 페이드 그라데이션 적용으로 시각적 완성도 향상
- **예외 처리**: 등록된 후기가 없을 시 노출되는 깔끔한 Empty State 플레이스홀더 제공

---

## [v1.2.2] - 2026-06-16 ✨

검색 엔진 최적화(SEO) 강화, AI 크롤러 차단 룰 및 사이트 학습용 지침서(ai.txt, llms.txt)가 도입되었습니다.

### Added

- **AI 및 LLM 지침 파일**: AI 봇이 참고할 수 있도록 `/ai.txt` 및 `/llms.txt` 정적 지침 파일을 루트에 배치
- **검색 크롤링 자동화**: 정적 핵심 URL을 담아 검색 엔진 수집을 돕는 `/sitemap.xml` 및 AI 봇(GPTBot, ClaudeBot, Google-Extended) 차단 지침을 포함하는 `/robots.txt` 적용

### Changed

- **SEO 및 메타데이터**: 메인 타이틀 정교화 및 OpenGraph 태그를 `https://zup-zup.com` 도메인 기준으로 최적화

---

## [v1.2.1] - 2026-06-13 ✨

검색 페이지 첫 진입 시 핵심 사용법을 짧게 안내하는 가이드 모달이 추가되었습니다.

### Added

- **강의 검색 가이드 모달**: `/search` 페이지에 처음 들어오면 검색 방법, 필터 사용법, 상세 확인 흐름을 짧게 안내하고 닫을 수 있는 모달을 추가
- **재열람 버튼**: 사용자가 원할 때 검색 화면에서 가이드를 다시 열 수 있도록 `사용법` 버튼 제공

---

## [v1.2.0] - 2026-03-23 🚀

서비스 브랜딩을 '전북대 수강신청 도우미'에서 **'줍줍 (zup-zup)'**으로 전면 개편했습니다.

### Changed
- **UI 및 메타데이터**: 메인 타이틀, 헤더 로고, 푸터 카피라이터, 로그인 카드 및 약관의 서비스 명칭을 **'줍줍'**으로 전면 교체
- **PWA 매니페스트**: `manifest.json`의 앱 설치명을 `줍줍`으로 일치화
- **SEO 최적화**: 페이지 타이틀 구조를 `줍줍 (zup-zup)`으로 일관성 있게 구성

---

## [v1.1.1] - 2026-03-10 🛠️

서비스 전반의 용어 정제 및 관리자 기능 고도화 작업이 진행되었습니다.

### Added

- **운영진 답변 삭제**: 관리자 페이지에서 잘못 작성되거나 불필요한 답변을 영구 삭제하는 기능 및 액션 로그 연동
- **이미지 외부 접근 자동화**: 업로드된 첨부 파일을 인증 없이도 안전하게 조회할 수 있도록 보안 정책 및 프록시 레이어 구축

### Changed

- **용어 전면 개편**: "피드백(Feedback)"으로 표기되던 모든 명칭을 사용자 친화적인 **"문의 및 건의"**로 통일 (UI, API, 주석 포함)
- **어드민 인터페이스 정교화**:
  - 처리 상태(대기/처리중/완료/반려) 및 문의 유형(버그/건의/기타)을 직관적인 한국어 배지로 대치
  - 답변 작성 시 실시간 통계 반영 및 수정 모드 UI 시인성 보완
- **개인정보 노출 최소화**: 일반 문의 목록 및 상세 뷰에서 작성자의 환경 정보(OS, 브라우저)를 숨기고 관리자 페이지에서만 확인 가능하도록 UX 조정

---

## [v1.1.0] - 2026-03-09 🚀

강의 리뷰 인터페이스 고도화 및 통합 건의/제보 시스템이 도입되었습니다.

### Added

- **통합 건의/제보 시스템(Feedback System)**: 버그 리포트, 기능 제안 등을 서비스 내부에서 직접 제출할 수 있는 인터페이스 추가
- **이미지 최적화 업로드**: 브라우저 단에서 JPEG/PNG만 선택·검증하고 동일 허용 포맷으로 리사이징·압축해 모바일 환경에서도 부담을 줄인다.
- **피드백 이력 관리**: 본인이 남긴 문의 내역을 탭 형태로 즉시 확인하고, 관리자의 답변을 실시간으로 피드백 받을 수 있는 UI 구축
- **관리자 전용 대시보드 확장**: 제보된 피드백의 상태 변경(대기/처리중/완료) 및 답변 등록, 수정 기능이 포함된 대규모 관리 인터페이스 도입
- **환경 메타데이터 자동 수집**: 제보 시점의 OS, 브라우저, 발생 경로 정보를 함께 전송하여 개발팀의 정확한 재현 및 디버깅 지원
- **리뷰 섹션(Review Section)**: 강의 상세 모달 하단에 별점 및 리뷰 목록 인터페이스 추가
- **사용자 중심 정렬**: 내가 작성한 리뷰가 리스트의 가장 앞에 고정되어 즉시 확인 가능하도록 UX 개선
- **반응형 리액션 버튼**: 각 리뷰에 대해 공감/비공감 피드백을 남길 수 있는 인터랙티브 버튼 도입
- **프리미엄 알림(Sonner)**: 브라우저 기본 알럿을 지양하고 `sonner` 라이브러리를 통한 세련된 토스트 메시지 시스템 구축
- **로그인 유도 시스템**: 비인증 사용자가 리뷰 관련 액션 수행 시 로그인 모달을 즉시 호출하도록 개선

### Changed

- **내비게이션 바 메뉴 개편**: 헤더 영역에 '건의사항' 메뉴를 상시 노출하여 접근성 극대화
- **소프트 삭제 정책 도입**: 유저가 삭제한 피드백을 데이터베이스에 안전하게 보존하면서 목록에서는 즉시 제거하여 데이터 무결성 확보
- **작성 폼 노출 최적화**: 중복 리뷰 작성 시 안내 배너 대신 작성 폼을 자동 은닉 처리하여 UI 노이즈 최소화
- **강의 상세 카드 디자인**: 우측 하단에 해당 강의의 평균 별점 및 리뷰 수를 실시간 반영하도록 레이아웃 보완
- **용어 일관성 정비**: 서비스 전반의 "한줄평" 명칭을 "리뷰"로 통일
- **PWA 설치 접근성**: 비로그인 상태에서도 홈 화면 바로가기/설치 버튼이 노출되도록 정책 변경
- **데이터 동기화 고도화**: TanStack Query를 통해 리뷰 및 피드백 액션 후 정보를 지연 없이 갱신

---

## [v1.0.0] - 2026-03-01 ✅

줍줍 웹 플랫폼의 모든 핵심 인터페이스를 통합한 정식 릴리스입니다.

### Added

- **그리드 기반 강의 검색**: 드래그 앤 드롭으로 시간을 직접 선택하거나 공강 기간을 자동 반영하는 스마트 필터
- **실시간 무한 스크롤**: TanStack Query를 활용하여 수천 개의 강의 데이터를 끊김 없이 탐색
- **공지사항(Announcement) 페이지**: 목록(미리보기)과 상세(마크다운 렌더링) 라우트 분리 및 검색 기능
- **공지사항 API bounded pagination**: public/admin 목록과 제목·내용 검색은 최대 100건 페이지와 100자 검색어 상한을 사용하고, `announcement.search.latency` metric으로 검색 지연을 관찰한다. 웹 API 계층은 `content`를 기존 배열 화면 모델로 변환해 기존 UI를 유지한다.
- **강의 검색 입력 경계**: 검색 조건은 문자열·배열·시간·학점·정렬 whitelist를 Bean Validation으로 제한하고, 분류/요일/공개 상태 등 enum 변환 실패는 쿼리에서 무시하지 않고 `400/G002`로 거부한다.
- **핵심 흐름 CI E2E**: Playwright browser smoke가 401→refresh→재시도와 관리자 첨부파일 preview를 mock API로 검증하고, 주간/manual provider·auth·outbox·DLQ·feedback contract suite는 별도 artifact로 보관한다.
- **통합 관리자 패널**: Recharts 기반의 통계 가시화, 크롤링 타겟 제어, 학사 일정 CRUD 화면
- **시간표 시뮬레이터**: 수업 시간표 생성 및 겹치는 강의 세로 분할 렌더링 로직 구현
- **Self-Healing 웹푸시**: VAPID 키 불일치나 만료된 구독을 자동으로 탐지하고 소생시키는 로직
- **카카오맵 SDK 연동**: 강의실 위치 정보를 지도에 즉각 매핑하여 시각화
- **사용자 온보딩**: 신규 가입 시 알림 채널 설정 및 필수 정보 입력을 돕는 단계별 가이드
- **공용 마크다운 렌더러**: `MarkdownViewer` 컴포넌트를 표준 UI로 도입하여 문서 렌더링 규칙 통일

### Changed

- **관리자 공지 편집 페이지 개편**: 목록 탐색과 편집/미리보기를 4:6 분할 레이아웃으로 최적화
- **마이페이지 알림 설정**: 10초 쿨타임 및 카운트다운을 포함한 실제 동작 테스트 버튼 제공
- **반응형 디자인 보완**: 모바일 환경에서의 시간표 스케일, 폰트 및 정렬 가독성 정밀 보정
- **검색 UX 최적화**: 불필요한 검색 버튼 및 중복 인터랙션을 제거한 직관적인 필터 배치


## Server Release Notes

`server` 모듈의 정식 릴리스 기능 명세입니다.

---

## [v1.4.1] - 2026-05-04 🚀

계절학기 대응을 위해 크롤링 주기를 최적화하고, 학기 관리 체계를 DB 중심으로 완전히 전환했습니다.

### Changed
- **크롤링 주기 최적화**: 수강신청 및 계절학기 정원 변동에 기민하게 대응하기 위해 크롤링 주기를 **1분**(`0 * * * * ?`)으로 단축했습니다.
- **DB 중심 학기 관리 체계 전환**: 설정 파일(`application.yml`)에 의존하던 `default-year`, `default-semester` 설정을 제거하고, 오직 DB(`crawler_settings`)를 통해서만 타겟을 관리하도록 리팩토링했습니다. (JbnuCourseApiClient 포함 전 계층 적용)
- **코드 안정성 강화**: 크롤링 타겟 조회 시 DB 데이터가 없을 경우를 대비한 방어 로직과 관련 단위 테스트(`CourseCrawlerTargetServiceTest`)를 추가했습니다.

### Added
- **크롤링 설정 부재 에러 추가**: DB에 크롤링 설정이 없을 경우 발생하는 `CRAWLER_SETTING_NOT_FOUND` 에러 코드를 추가하여 시스템 안정성을 높였습니다.

## [v1.4.0] - 2026-04-26 🚀

프로젝트 전반의 DTO 구조를 표준화하고 계층 간 결합도를 낮추기 위한 대규모 리팩토링을 진행했습니다.

### Added
- **ParsedCourseDto 도입**: 파서와 서비스 간 데이터 전달을 위한 전용 DTO를 추가하여 도메인 엔티티와 외부 데이터 간의 결합도를 완화했습니다.
- **CrawlTargetInfo 통합**: 내부적으로 흩어져 있던 학기 정보를 `CrawlTargetInfo`로 통합하여 관리 효율성을 높였습니다.

### Changed
- **DTO 패키지 구조 전면 재편**: 모든 도메인에서 `dto` 폴더를 제거하고, `request`와 `response` 패키지로 명확히 분리하여 데이터 흐름을 직관적으로 개선했습니다.
- **학기(Semester) 관리 체계 고도화**: `SemesterType` Enum을 도입하고 학기 코드를 타입 안전하게 처리하도록 로직을 강화했습니다.
- **크롤링 파이프라인 정규화**: `JbnuCourseParser`가 엔티티를 직접 생성하던 방식에서 DTO를 반환하도록 변경하여 유지보수성을 향상시켰습니다.

### Fixed
- **DTO 명칭 및 위치 혼선 해결**: 산재해 있던 내부 record들을 외부 클래스로 추출하고 명칭을 표준화하여 개발 생산성을 높였습니다.

---

## [v1.3.1] - 2026-04-26 🚀

전북대학교 학과 매핑률 100%를 달성하고, 향후 조직 개편에 유연하게 대응하기 위한 자동화 시스템 기반을 구축했습니다.

### Added
- **AI대학 명칭 반영**: 2026학년도 신설된 'AI대학' 명칭 및 소속 학과(소프트웨어공학과, 컴퓨터인공지능학부) 데이터 최신화
- **대학원 전용 학과 필터링**: 학부 수강신청 시스템에서 제외되는 대학원 및 특수 학과(의과학과, 치의과학과 등) 자동 제외 로직 도입
- **미매핑 학과 추적 및 수집**: 크롤링 중 발생하는 미매핑 데이터를 실시간으로 감지하고 관리할 수 있는 프로세스 구축

### Changed
- **별칭 사전(ALIAS_MAP) 전면 개편**: 100개 이상의 신규 학과 별칭 및 구 명칭 매핑 추가 (농생물학->농생물학과, 임학->산림환경과학과 등)
- **DB 스키마(V4) 고도화**: 단과대 및 학과 테이블의 데이터를 2026학년도 공식 조직도 기준으로 전수 동기화
- **H2 호환성 강화**: 로컬 개발 환경(H2)에서도 마이그레이션이 완벽히 작동하도록 SQL 문법 최적화

### Fixed
- **특수 학과 매핑 누락**: `농생물학과`, `약학과`, `윤리교육과` 등 일부 필수 학과가 DB에서 누락되어 매핑에 실패하던 현상 해결
- **캠퍼스 중복 학과 구분**: 익산(생태조경디자인)과 전주(조경) 캠퍼스 간의 명칭 중복 및 매핑 오류 수정

---

## [v1.3.0] - 2026-04-26 🚀

---

## [v1.2.0] - 2026-03-23 🚀

서비스 브랜딩을 '전북대 수강신청 도우미'에서 **'줍줍 (zup-zup)'**으로 전면 개편했습니다.

### Added
- **도메인 설정**: `www.zup-zup.com` 도메인 연동을 위한 CORS 정책 및 OAuth 리다이렉트 URI 반영 (`application-prod.yml`)

### Changed
- **앱 명칭 변경**: `application.yml` 내 `spring.application.name`을 `zup-zup`으로 변경
- **알림 타이틀**: 발송되는 알림의 말머리를 `[전북대 수강신청 도우미]` ➡️ **`[줍줍]`** 으로 변경
- **크롤러 식별자**: K6 성능 테스트용 User-Agent 명칭 변경 (`Zup-Zup-Bot`)

---

## [v1.1.0] - 2026-03-09 🚀

강의 리뷰 시스템 고도화 및 통합 건의/제보 백엔드 인프라가 구축되었습니다.

### Added

- **통합 건의/제보(Feedback) 시스템**: 사용자 피드백 수집 및 관리자 답변 처리를 위한 도메인 모델(Entity, Repository) 구축
- **안전한 멀티파트 파일 업로드**: `Apache Tika` 라이브러리를 활용한 파일 시그니처 정밀 검증 및 UUID 기반 로컬 저장 시스템 구현
- **관리자 액션 로그(AdminActionLog)**: 상태 변경, 답변 등록 등 관리자의 주요 행위를 추적하기 위한 데이터 감사 시스템 도입
- **데이터베이스 스키마 확장**: 피드백, 첨부파일, 답변 관리용 테이블 추가 반영 (V2 migration 최적화)
- **강의 리뷰(Review) 시스템**: 강의별 1~5점 별점 및 익명 리뷰 작성 기능 구현
- **리뷰 반응(Reaction) 기능**: 리뷰별 공감(LIKE) / 비공감(DISLIKE) 토글 시스템 구축
- **사용자 맞춤형 리뷰 정렬**: 리뷰 목록 조회 시 현재 로그인한 사용자의 리뷰를 최상단에 자동 고정하는 쿼리 로직 반영
- **리뷰 통계 자동 집계**: 리뷰 작성/삭제 시 해당 강의의 평균 별점 및 리뷰 수 실시간 자동 갱신

### Changed

- **로컬 개발 데이터 영속화**: H2 데이터베이스를 `file` 모드로 전환하여 서버 재시작 시에도 데이터가 유지되도록 구성 (`./data/jbnu-sugang`)
- **인증 로직 고도화**: `SecurityContextHolder`를 활용한 서비스 레이어 내 인증 정보 획득 방식 정규화
- **DTO 명세 일관성**: 모든 Response DTO에서 Fully Qualified Name 제거 및 표준 Import 방식 통일
- **테스트 커버리지 및 패턴 최적화**: `Given/When/Then` 기반의 BDD 테스트 패턴 적용 및 중복 모킹 로직 helper 메서드화
- **로그 및 에러 메시지 표준화**: 모든 로그 메시지를 영문으로 통일하고 예외 메시지의 가독성 개선
- **소프트 삭제(Soft Delete) 정책**: 피드백 및 답변 데이터 삭제 시 물리적 삭제 대신 `deleted_at` 컬럼을 활용한 논리 처리 적용

---

## [v1.0.0] - 2026-03-01 ✅

줍줍 서버의 모든 핵심 기능을 통합한 정식 릴리스입니다.

### Added

- **OASIS 크롤링 파이프라인**: 전북대 오아시스 XML 수집 및 파싱 클라이언트 구축
- **멀티 채널 알림 엔진**: EMAIL, Web Push(FCM), Discord 연동 발송기 구현
- **실시간 여석 감지**: `SeatOpenedEvent` 기반의 비동기 알림 트리거 시스템
- **알림 중복 방지(Dedup)**: Redis 기반 10분 이내 동일 과목 알림 차단 로직 적용
- **공지사항(Announcement) 시스템**: 제목/내용 통합 검색 및 목록/상세 DTO 분리 조회 API
- **크롤링 타겟 동적 관리**: 관리자 전용 연도/학기 설정 및 수동 크롤링 트리거 엔드포인트
- **정밀 시간 검색 모델**: `dayOfWeek`, `startTime`, `endTime` 기반의 시간 범위 검색 엔진
- **인증 및 온보딩**: Google OAuth2 연동 및 및 Refresh Token 보안 세션 관리
- **관리자 대시보드 API**: 시스템 가입자, 알림 발송 현황 통계 및 실시간 로그 조회

### Changed

- **DB 마이그레이션 통합**: 산재된 스키마 변경 이력을 `V3__consolidate_schema_updates.sql`로 최적화 및 통합
- **테스트 알림 정책**: 사용자별 10초 쿨타임 제한 및 운영 통계(히스토리) 제외 처리 적용
- **도메인 모델 정규화**: 불필요한 `period` 필드 제거 및 시간표 검색 로직 고도화
